# $TERM required field
# &TERM optional field
# +TERM you can specify TERM in [] - array - to include variable amount of TERMs

WIDGET := {
    $type := TYPE
    &layout := LAYOUT
    # you need to specify \/ if ^ specified and visa versa
    &childWidgets := +WIDGET
    TERM := TERM
    VAR := TERM
}

TYPE := widgetType
LAYOUT := widgetLayout
TERM := any json value
NAME := any string
VAR := "*" + NAME
